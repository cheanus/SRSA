[project]
name = "srsa"
version = "0.1.0"
description = "Spaced Repetition Systems for AI Agents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "fsrs>=6.3.0",
  "PyYAML>=6.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
